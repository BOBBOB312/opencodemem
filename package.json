{
  "name": "opencodemem",
  "version": "0.1.0",
  "description": "Persistent memory system for OpenCode - preserves context across sessions",
  "keywords": [
    "opencode",
    "plugin",
    "memory",
    "persistent",
    "context",
    "ai",
    "coding-agent"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "type": "module",
  "main": "dist/plugin.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bunx tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"src/**/*.{ts,js}\"",
    "format:check": "prettier --check \"src/**/*.{ts,js}\"",
    "worker:start": "bun run src/services/worker/server.ts",
    "test": "bun test tests/config.test.ts && bun test tests/context-builder.test.ts && bun test tests/context-inject-budget.test.ts && bun test tests/integration.test.ts && bun test tests/migrations.test.ts && bun test tests/privacy.test.ts && bun test tests/ranker.test.ts && bun test tests/search-diagnostics.test.ts",
    "test:e2e": "bun test tests/e2e.worker-api.test.ts",
    "test:reliability": "bun test tests/reliability.queue-retry.test.ts && bun test tests/reliability.idempotency.test.ts",
    "test:perf": "bun test tests/performance.search-latency.test.ts && bun test tests/performance.long-run.test.ts",
    "test:chaos": "bun run test:reliability",
    "test:all": "bun run test && bun run test:reliability && bun run test:perf && bun run test:e2e"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.162",
    "express": "^4.18.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "bun-types": "^1.0.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "opencode": {
    "type": "plugin",
    "hooks": [
      "chat.message",
      "event"
    ]
  },
  "files": [
    "dist"
  ]
}
